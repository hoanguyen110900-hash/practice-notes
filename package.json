{
  "name": "practice_backend",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "uyen",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../practice_frontend && npm run build && cp -r dist ../practice",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
